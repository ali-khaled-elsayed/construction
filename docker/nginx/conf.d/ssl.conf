# =============================================================================
# SSL-ready server block (staging/production)
#
# Mount certificates:
#   ./docker/nginx/certs/fullchain.pem  -> /etc/nginx/certs/fullchain.pem
#   ./docker/nginx/certs/privkey.pem    -> /etc/nginx/certs/privkey.pem
#
# Let's Encrypt example (on host, not in container):
#   certbot certonly --webroot -w ./construction-marketplace-api/public -d api.example.com
# =============================================================================

# Uncomment and include this file from default.conf or merge after obtaining certs.
# server {
#     listen 443 ssl http2;
#     server_name api.example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#
#     # HSTS (enable only when HTTPS is fully working)
#     # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     root /var/www/html/public;
#     index index.php;
#
#     include /etc/nginx/snippets/security-headers.conf;
#
#     location /api { try_files $uri $uri/ /index.php?$query_string; }
#     location /admin { try_files $uri $uri/ /index.php?$query_string; }
#     location /storage { alias /var/www/html/public/storage; }
#     location ~ \.php$ { include /etc/nginx/snippets/fastcgi-php.conf; }
#
#     location / {
#         proxy_pass http://nextjs_frontend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }
#
# server {
#     listen 80;
#     server_name api.example.com;
#     return 301 https://$host$request_uri;
# }
